# URL del backend (server-side, no expuesto al navegador)
BACKEND_URL=http://localhost:3001

# TODO: reemplazar por sesión real cuando exista login (tabla `usuarios`).
# Mientras tanto, un solo tenant fijo por variable de entorno.
NEXT_PUBLIC_EMPRESA_ID=
